curriculum:
  measure: reward
  thresholds: [0.5, 0.8]  # When average reward per episode passes this, move to next lesson
  min_lesson_length: 100
  signal_smoothing: true
  parameters:
    maze_difficulty: [0, 1, 2]